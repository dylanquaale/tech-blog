{{!-- {{> post-info post}} --}}

{{!-- {{#if logged_in}}
<div class="card">
    <h3 class="card-header"></h3>
    <form class="card-body comment-card" id="comment-card">

        <label class="form-label">Comment</label>
        <input id="comment" name="comment-body" class="form-input" />

        <button type="submit" class="btn">Submit</button>
    </form>
</div>
{{/if}}

{{> comments post.comments}}

{{#if logged_in}}
<script src="/js/comment.js"></script>
{{/if}} --}}

{{!-- {{#if logged_in}}

		<form class="new-comment-form formCont d-flex flex-wrap justify-content-center" data-postid={{id}}>
			<div class="textAreaCont p-2">
				<textarea id='comment_text' name="comment-body"
				placeholder="Enter a comment here..."
				cols="30"
				rows="2"></textarea>
			</div>
			<div class="btnComment p-2">
				<button class="button btn btn-success" type="submit">Add Comment</button>
			</div>
		</form>

		{{/if}}

        {{> comments post.comments}}

		<h2 class="text-center">Comments</h2>
		{{#each comments as |comment|}}
		<div class="commentCont">
			<p class="text-center commentText">{{comment.comment_text}}</p>
			<p class="text-center commentOwner"><span>~</span>Created by {{comment.user.username}} on {{format_date date_created}}<span>~</span></p>
		</div>
		{{/each}}

        <script src="/js/comments.js"></script> --}}